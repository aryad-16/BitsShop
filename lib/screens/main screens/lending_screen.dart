import 'package:flutter/material.dart';

class LendingScreen extends StatelessWidget {
  const LendingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Lending Screen'),
      ),
    );
  }
}
